<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen teması tanımlanıyor -->
    <style name="Theme.MySplashScreen" parent="Theme.SplashScreen">
        <!-- Splash Screen arka plan rengi -->
        <item name="windowSplashScreenBackground">@color/black</item>
        <!-- Splash Screen'da gösterilecek animasyonlu logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/card2qr_splash_screen_logo</item>
        <!-- Splash Screen sonrası geçilecek tema -->
        <item name="postSplashScreenTheme">@style/Theme.Card2QR</item>

    </style>
</resources>
